services:
  docchat:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    environment:
      - DB_FILE=/app/storage/chatbot.db
      - INDEX_BASE_DIR=/app/storage/faiss_index
    volumes:
      - app_storage:/app/storage

volumes:
  app_storage:
